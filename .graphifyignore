# graphify exclusions — keep tests out of the knowledge graph.
# gitignore syntax; merged with .gitignore (last-match-wins). Patterns are
# unanchored, so a bare name matches that path component at any depth.

# Test directories across all components
# (backend, agent, testkit, router, scripts/state-testing, driver-packs adapters)
tests/

# Frontend Playwright end-to-end suite
e2e/

# Test files colocated outside tests/ dirs
conftest.py
test_*.py
*_test.py
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
